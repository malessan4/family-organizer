package com.familyorganizer.backend.model;

public enum Role {
    MEMBER,   // Rol por defecto para todos los miembros de la familia
    PARENT,   // Reservado para uso futuro (permisos de administrador)
    CHILD     // Reservado para uso futuro (permisos limitados)
}
